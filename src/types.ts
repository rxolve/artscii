export interface ArtEntry {
  id: string;
  name: string;
  category: string;
  tags: string[];
  file: string;
}

export interface ArtResult {
  id: string;
  name: string;
  category: string;
  tags: string[];
  art: string;
}
